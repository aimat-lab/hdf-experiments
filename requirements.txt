# Pinned third-party dependencies for the HDF artifact, captured from the
# development environment (Python 3.11.10). `torch` is installed separately in
# the Dockerfile from the CPU wheel index; `chem_mat_database` is installed from
# git (see Dockerfile). These pins reproduce the exact software stack used to
# generate the paper's results.
torch-geometric==2.6.1
pytorch-lightning==2.5.2
pycomex==0.28.0
rdkit==2025.9.1
scikit-learn==1.6.1
numpy==1.26.4
scipy==1.16.3
pandas==2.2.1
botorch==0.16.0
gpytorch==1.14.2
matplotlib==3.10.7
seaborn==0.13.2
umap-learn==0.5.7
imbalanced-learn==0.13.0
normflows==1.7.3
networkx==3.3
jsonpickle==4.1.1
python-decouple==3.8
rich-click==1.8.9
click==8.3.0
jinja2==3.1.6
