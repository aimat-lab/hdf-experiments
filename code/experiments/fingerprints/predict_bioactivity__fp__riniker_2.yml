pycomex: true
extend: predict_bioactivity__fp.py
parameters:
  # Dataset parameters
  DATASET_NAME: "riniker_2"
  DATASET_NAME_ID: "riniker_2"
  NUM_DATA: null  # Use full dataset (20,922 molecules)
  SEED: 1

  # Evaluation parameters
  NUM_QUERY_ACTIVES: 5        # Number of active queries per repetition
  NUM_REPETITIONS: 50          # Statistical repetitions per target
  MIN_ACTIVES_PER_TARGET: 10   # Targets have 79-445 actives

  # Metric parameters
  BEDROC_ALPHA: 20.0          # Standard α=20 for virtual screening
  EF_PERCENTAGES: [0.01, 0.05]  # EF at 1% and 5%

  # Fingerprint parameters
  FP_TYPE: "ecfp4"            # ECFP4 (Morgan radius=2)
  FP_SIZE: 2048               # 2048 bits
  FP_RADIUS: 2                # Radius for Morgan fingerprint
  USE_COUNTS: false           # Binary fingerprint

  # Visualization
  NUM_EXAMPLE_TARGETS: 3
  FIGURE_DPI: 300

  NOTE: "ECFP4 baseline for bioactivity prediction on Riniker dataset 2 with 37 targets and 10k decoys per target"
