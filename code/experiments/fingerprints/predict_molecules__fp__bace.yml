# PyComex configuration file for BACE dataset fingerprint experiments
pycomex: true

extend: predict_molecules__fp.py

name: fp_bace

description: |
  Fingerprint-based molecular property prediction on the BACE dataset.

  BACE (Beta-secretase 1) is a binary classification dataset for binding results for
  inhibitors of human β-secretase 1 (BACE-1).

parameters:
  DATASET_NAME: "bace"
  DATASET_NAME_ID: "bace"
  DATASET_TYPE: "binary"
  NUM_TEST: 0.1
  FINGERPRINT_SIZE: 2048
  FINGERPRINT_RADIUS: 2
